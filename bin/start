#!/usr/bin/env bash
# Start Evo in production mode
set -euo pipefail
cd "$(dirname "$0")/.."

if [ -f .env ]; then
  set -a
  source .env
  set +a
fi

echo "Starting Evo at http://$(hostname -I | awk '{print $1}'):${PORT:-4000}/evolution"
MIX_ENV=prod elixir --sname evo -S mix phx.server
